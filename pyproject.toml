[project]
name = "comfyui-helpers-nodes"
description = "A small pack of ComfyUI helper nodes."
version = "0.2.0"
requires-python = ">=3.9"
dependencies = ["Pillow", "numpy"]

[project.urls]
Repository = "https://github.com/comandeur/COMFYUI_helpers_nodes"

[tool.comfy]
PublisherId = "comandeur"
DisplayName = "COMFYUI helpers nodes"
Repository = "https://github.com/comandeur/COMFYUI_helpers_nodes"
